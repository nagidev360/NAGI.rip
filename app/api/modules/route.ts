import { NextRequest,NextResponse } from "next/server";
import { z } from "zod";
import { createClient } from "@/lib/supabase/server";
const schema=z.object({type:z.enum(["TEXT","HEADING","IMAGE","VIDEO","BUTTON","LINK","SOCIALS","DISCORD","ROBLOX","MUSIC","SPOTIFY","YOUTUBE","TWITCH","GITHUB","COUNTDOWN","DIVIDER","SPACER","QUOTE","STATS","LOCATION","CONTACT","EMAIL","EMBED","ANNOUNCEMENT","GALLERY","FAQ","DONATION","SUPPORT","CALENDAR","GAME_PROFILE"]),position:z.number().int().min(0).default(0),settings:z.record(z.string(),z.unknown()).default({}),visibility:z.boolean().default(true)});
async function ctx(){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)return {supabase,user:null,profile:null};const {data:profile}=await supabase.from("profiles").select("id").eq("user_id",user.id).maybeSingle();return {supabase,user,profile};}
export async function GET(){const {supabase,user,profile}=await ctx();if(!user||!profile)return NextResponse.json({error:"Unauthorized"},{status:401});const {data,error}=await supabase.from("modules").select("*").eq("profile_id",profile.id).order("position");if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({modules:data});}
export async function POST(req:NextRequest){const {supabase,user,profile}=await ctx();if(!user||!profile)return NextResponse.json({error:"Unauthorized"},{status:401});const parsed=schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid module."},{status:400});const {data,error}=await supabase.from("modules").insert({profile_id:profile.id,...parsed.data}).select("*").single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({module:data},{status:201});}
export async function DELETE(req:NextRequest){const {supabase,user,profile}=await ctx();if(!user||!profile)return NextResponse.json({error:"Unauthorized"},{status:401});const id=new URL(req.url).searchParams.get("id");if(!id)return NextResponse.json({error:"Missing id."},{status:400});const {error}=await supabase.from("modules").delete().eq("id",id).eq("profile_id",profile.id);if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({ok:true});}
